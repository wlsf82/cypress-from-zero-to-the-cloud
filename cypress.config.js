const { defineConfig } = require('cypress')

module.exports = defineConfig({
  // video: true,
  viewportHeight: 880,
  viewportWidth: 1280,
  // viewportHeight: 375,
  // viewportWidth: 667, 
  e2e: {},
})
